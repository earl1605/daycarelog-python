# Django
DJANGO_SECRET_KEY=change-me-to-a-long-random-string
# DEBUG now defaults to False (production-safe) - local dev must set this explicitly.
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
DJANGO_CSRF_TRUSTED_ORIGINS=

# Supabase PostgreSQL - use the Transaction Pooler connection details from
# Supabase Project Settings > Database > Connection Pooling.
DB_NAME=postgres
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=6543
DB_SSLMODE=require
